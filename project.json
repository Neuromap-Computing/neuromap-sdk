{
  "name": "neuro-sim",
  "root": "neuro-sim",
  "sourceRoot": "neuro-sim/src",
  "projectType": "library",
  "tags": ["lang:python", "type:sdk"],
  "targets": {
    "install": {
      "executor": "nx:run-commands",
      "options": { "command": "uv sync", "cwd": "neuro-sim" },
      "cache": false
    },
    "build": {
      "executor": "nx:run-commands",
      "options": { "command": "uv build", "cwd": "neuro-sim" },
      "outputs": ["{projectRoot}/dist"]
    },
    "test": {
      "executor": "nx:run-commands",
      "options": { "command": "uv run pytest tests/ -v", "cwd": "neuro-sim" }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": { "command": "uv run ruff check .", "cwd": "neuro-sim" },
      "inputs": ["default", "{workspaceRoot}/pyproject.toml"]
    },
    "format": {
      "executor": "nx:run-commands",
      "options": { "command": "uv run ruff format .", "cwd": "neuro-sim" }
    },
    "format-check": {
      "executor": "nx:run-commands",
      "options": { "command": "uv run ruff format --check .", "cwd": "neuro-sim" },
      "inputs": ["default", "{workspaceRoot}/pyproject.toml"]
    },
    "serve-docs": {
      "executor": "nx:run-commands",
      "options": { "command": "uv run mkdocs serve", "cwd": "neuro-sim" },
      "cache": false
    }
  }
}
